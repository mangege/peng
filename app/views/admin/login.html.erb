<%= flash[:notice] %>
<%= form_tag do %>
    <div>
    <table>
      <tr>
        <td><%= label_tag(:username, "用户名:") %></td>
        <td><%= text_field_tag(:username,params[:username]) %></td>
      </tr>
      <tr>
        <td><%= label_tag(:password,"密  码:") %></td>
        <td><%= password_field_tag(:password) %></td>
      </tr>
      <tr>
        <td align="center" colspan="2"><%= submit_tag :登陆 %></td>
      </tr>
    </table>
    </div>
<% end %>